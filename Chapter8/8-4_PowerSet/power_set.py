def power_set(superset):
    superlist = list(superset)
    to_return = list()

    def recursion(start):
        if start >= len(superlist):
            return



    for ind, num in enumerate(superset):

    return None



case1 = set([1, 2, 3, 4, 5])

power_set()